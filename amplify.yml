version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 16
        - nvm use 16
        - node -v
        - npm -v
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build   # CRA
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
